"use client";
import Image from "next/image"; import Link from "next/link"; import { useSearchParams } from "next/navigation"; import { useEffect, useMemo, useState } from "react"; import { Building2, LoaderCircle, MoreHorizontal, PackageCheck, Pencil, Plus, Search, Tag } from "lucide-react"; import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from "@/components/ui/dropdown-menu"; import { mediaFileUrl } from "@/lib/media";
type Item={id:number;title:string;slug:string;status:"ACTIVE"|"INACTIVE";_count:{products:number};city?:string|null;countryCode?:string;logo?:string|null};
export function BrandsSuppliersPage(){const params=useSearchParams(), tab=params.get("tab")==="suppliers"?"suppliers":"brands",[brands,setBrands]=useState<Item[]>([]),[suppliers,setSuppliers]=useState<Item[]>([]),[loading,setLoading]=useState(true),[q,setQ]=useState("");useEffect(()=>{const timer=window.setTimeout(async()=>{setLoading(true);const [b,s]=await Promise.all([fetch("/api/catalog/brands"),fetch("/api/catalog/suppliers")]);const [bp,sp]=await Promise.all([b.json(),s.json()]);const brandRows=Array.isArray(bp)?bp:Array.isArray(bp.data)?bp.data:bp.data?.items??bp.items??[];const supplierRows=Array.isArray(sp)?sp:Array.isArray(sp.data)?sp.data:sp.data?.items??sp.items??[];setBrands(brandRows);setSuppliers(supplierRows);setLoading(false)},0);return()=>clearTimeout(timer)},[]);const items=tab==="brands"?brands:suppliers,shown=useMemo(()=>items.filter(i=>!q||`${i.title} ${i.slug} ${i.city??""}`.toLowerCase().includes(q.toLowerCase())),[items,q]);async function toggle(i:Item){const next=i.status==="ACTIVE"?"INACTIVE":"ACTIVE",endpoint=tab==="brands"?"brands":"suppliers";await fetch(`/api/catalog/${endpoint}/${i.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:next})});if(tab==="brands")setBrands(v=>v.map(x=>x.id===i.id?{...x,status:next}:x));else setSuppliers(v=>v.map(x=>x.id===i.id?{...x,status:next}:x))}return <div><div className="flex items-start justify-between"><div><p className="text-xs text-ink-muted">Catalog</p><h1 className="mt-2 text-[22px] font-semibold text-ink">Brands & suppliers</h1><p className="mt-1 text-[13.5px] text-ink-muted">Manage manufacturers and the companies that supply your catalogue.</p></div><Link href={tab==="brands"?"/brands/new":"/suppliers/new"} className="inline-flex h-10 items-center gap-2 rounded-md bg-ink px-4 text-[13px] font-semibold text-white"><Plus className="h-4 w-4"/>Add {tab==="brands"?"brand":"supplier"}</Link></div><div className="mt-5 flex border-b border-border"><Link href="/brands" className={`px-4 py-3 text-xs font-semibold ${tab==="brands"?"border-b-2 border-ink text-ink":"text-ink-muted"}`}>Brands</Link><Link href="/brands?tab=suppliers" className={`px-4 py-3 text-xs font-semibold ${tab==="suppliers"?"border-b-2 border-ink text-ink":"text-ink-muted"}`}>Suppliers</Link></div><div className="mt-4 grid gap-3 sm:grid-cols-3"><Metric icon={tab==="brands"?Tag:Building2} label={`Total ${tab}`} value={items.length}/><Metric icon={PackageCheck} label="Products assigned" value={items.reduce((n,i)=>n+i._count.products,0)}/><Metric icon={Building2} label="Enabled" value={items.filter(i=>i.status==="ACTIVE").length}/></div><section className="mt-4 overflow-hidden rounded-xl border border-border bg-surface shadow-card"><div className="flex items-center justify-between border-b border-border p-4"><div><h2 className="text-[13.5px] font-semibold text-ink">{tab==="brands"?"Brands":"Suppliers"} ({shown.length})</h2></div><label className="relative"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-ink-faint"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search by name" className="h-9 w-64 rounded-md border border-border-strong pl-9 pr-3 text-xs outline-none"/></label></div><table className="w-full min-w-[650px] text-left"><thead className="bg-canvas text-[10.5px] uppercase tracking-wide text-ink-muted"><tr><th className="px-4 py-3">ID</th><th className="px-4 py-3">Name</th>{tab==="suppliers"?<th className="px-4 py-3">Location</th>:null}<th className="px-4 py-3 text-center">Products</th><th className="px-4 py-3 text-center">Enabled</th><th className="px-4 py-3 text-right">Actions</th></tr></thead><tbody className="divide-y divide-border">{loading?<tr><td colSpan={6} className="h-36"><LoaderCircle className="mx-auto h-5 w-5 animate-spin"/></td></tr>:shown.map(i=><tr key={i.id} className="hover:bg-canvas"><td className="px-4 py-3 font-mono text-xs text-ink-muted">{i.id}</td><td className="px-4 py-3"><div className="flex items-center gap-3">{tab==="brands"?(i.logo?<span className="relative h-8 w-8 shrink-0 overflow-hidden rounded-md border border-border bg-neutral-tint"><Image src={mediaFileUrl(i.logo)} alt="" fill unoptimized sizes="32px" className="object-contain"/></span>:<span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-md border border-border bg-canvas text-[10.5px] font-semibold text-ink-muted">{i.title.slice(0,2).toUpperCase()}</span>):null}<p className="text-[13px] font-semibold text-ink">{i.title}</p></div></td>{tab==="suppliers"?<td className="px-4 py-3 text-xs text-ink-muted">{[i.city,i.countryCode].filter(Boolean).join(", ")||"—"}</td>:null}<td className="px-4 py-3 text-center text-xs font-semibold">{i._count.products}</td><td className="px-4 py-3 text-center"><button role="switch" aria-checked={i.status==="ACTIVE"} onClick={()=>void toggle(i)} className={`relative h-5 w-9 rounded-full ${i.status==="ACTIVE"?"bg-ink":"bg-border-strong"}`}><span className={`absolute left-0.5 top-0.5 h-4 w-4 rounded-full bg-white shadow-sm transition-transform ${i.status==="ACTIVE"?"translate-x-4":"translate-x-0.5"}`}/></button></td><td className="px-4 py-3 text-right"><DropdownMenu><DropdownMenuTrigger render={<button className="inline-flex h-8 w-8 items-center justify-center rounded-md"><MoreHorizontal className="h-4 w-4"/></button>}><span className="sr-only">Actions</span></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem render={<Link href={`/${tab}/${i.id}/edit`}/>}><Pencil />Edit {tab==="brands"?"brand":"supplier"}</DropdownMenuItem></DropdownMenuContent></DropdownMenu></td></tr>)}</tbody></table></section></div>}
function Metric({icon:Icon,label,value}:{icon:typeof Tag;label:string;value:number}){return <div className="rounded-xl border border-border bg-surface p-4 shadow-card"><Icon className="h-4 w-4 text-ink-muted"/><p className="mt-3 text-[10.5px] font-semibold uppercase tracking-wide text-ink-muted">{label}</p><p className="mt-1 text-xl font-semibold text-ink">{value}</p></div>}
