import { Tag } from "lucide-react";
import { ComingSoon } from "@/components/ui/coming-soon";

export default function BrandsPage() {
  return (
    <ComingSoon
      icon={Tag}
      title="Brands"
      description="Brand profiles and their product listings — built next on this same system."
    />
  );
}
