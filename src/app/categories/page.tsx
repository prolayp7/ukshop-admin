import { FolderTree } from "lucide-react";
import { ComingSoon } from "@/components/ui/coming-soon";

export default function CategoriesPage() {
  return (
    <ComingSoon
      icon={FolderTree}
      title="Categories"
      description="Category hierarchy and the per-category specification attributes buyers filter by — built next on this same system."
    />
  );
}
